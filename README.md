ChocoBits adds a single root command (\`/cb\` or \`/chocobits\`) with two core utilities:  
• Toggle custom features (e.g. narrator) at runtime    
• Edit both persisted and vanilla player NBT data live
